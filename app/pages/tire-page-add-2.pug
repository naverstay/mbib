extends ../blocks/layout-default/layout-auth
include ../blocks/breadcrumbs/breadcrumbs
include ../blocks/step/step-tire-2

block head
  - var pageTitle = 'Подать объявление о продаже шин 2';
  - var pageMod = 'Tires Page';
  - var innerClass = 'tires-page';

block content
  +breadcrumbs(getData('index-crumbs'))
  +step-tire-2
