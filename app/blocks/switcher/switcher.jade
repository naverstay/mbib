include ../btn/btn

mixin switcher()
	+b.LABEL.switcher&attributes()(for=attributes.id)
		+e.INPUT.control(type=attributes.type, name=attributes.name, value=attributes.value, id=attributes.id)
		+btn()(class=attributes.class role=attributes.type).switcher__text
			block
